discodeit:
  storage:
    type: local
    local:
      root-path: ./data/binary

spring:
  application:
    name: discodeit
  profiles:
    active: dev
  jpa:
    database: POSTGRESQL


    properties:
      hibernate:
        format_sql: true
    open-in-view: true

